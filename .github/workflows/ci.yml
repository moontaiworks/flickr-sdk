name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint-and-build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x, 22.x, 24.x]

    env:
      FLICKR_CONSUMER_KEY: ${{ secrets.FLICKR_CONSUMER_KEY }}
      FLICKR_CONSUMER_SECRET: ${{ secrets.FLICKR_CONSUMER_SECRET }}
      FLICKR_TOKEN: ${{ secrets.FLICKR_TOKEN }}
      FLICKR_TOKEN_SECRET: ${{ secrets.FLICKR_TOKEN_SECRET }}
      FLICKR_USER_ID: ${{ vars.FLICKR_USER_ID }}
      FLICKR_USERNAME: ${{ vars.FLICKR_USERNAME }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4
        name: Install pnpm
        with:
          version: 10
          run_install: false

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run style checks
        run: pnpm run '/^(lint|format:check|typecheck)$/'

      - name: Build project
        run: pnpm run build

      - name: Run tests with coverage
        run: npx -y vitest run --coverage

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/lcov.info
          fail_ci_if_error: false
